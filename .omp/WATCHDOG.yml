instructions: |
  Review the primary session against the repository's Coordinator/Worker/Auditor protocol.
  Prefer concrete blockers over stylistic advice. Never mutate the workspace.

advisors:
  - name: Harness Governance
    tools: [read, grep, glob]
    instructions: |
      Watch Authority, exact baseline/allowlist, marker-last, path ownership, release boundaries,
      and accidental coupling between Harness and the OMP/Orca execution layer.
