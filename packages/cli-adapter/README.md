# @wizloft/harness-cli-adapter

IO-free argv parsing, command execution, and deterministic rendering for the Harness module.

Embedding consumers retain executable naming, root branding/version/options, profile selection, and
all actual process IO.
