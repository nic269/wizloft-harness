# @wizloft/harness

Curated consumer-facing SDK facade over one explicitly composed Harness runtime.

The package owns no provider plugins, profile loader, process-global runtime, or executable binary.
