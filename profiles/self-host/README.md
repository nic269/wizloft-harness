# @wizloft/harness-profile-self-host

Project profile used by Gate B to operate Wizloft Harness on its own repository.

It composes only existing first-party capabilities/providers and owns two repository-specific
validators: accepted authority documentation and the root workspace verification contract. It is
not a base profile, profile loader, workflow engine, or executable CLI.
